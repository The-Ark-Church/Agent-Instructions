# Org Instructions

Source of truth for The Ark Church's Claude Teams org-level configuration.

Planned contents (Phase 2 of the AI Knowledge Base plan):

- `CLAUDE-ORG-INSTRUCTIONS.md` — the org instructions pasted into the Claude Teams admin console (character-limited; points agents at the knowledge-base files by raw GitHub URL)
- `CLAUDE-ORG-PROFILE.md` — the org profile (contextual background: mission, ministries, systems, seasonal context)

When either file changes here, an admin re-pastes it into the admin console — the console doesn't pull from GitHub automatically.
